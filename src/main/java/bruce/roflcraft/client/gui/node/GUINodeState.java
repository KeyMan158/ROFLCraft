package bruce.roflcraft.client.gui.node;

/**
 * Enum representing the three different states a node
 * can be in; Active (all detail is shown), Dormant (
 * only the frame is shown,Hidden (nothing is shown)
 * @author Lorrtath
 *
 */
public enum GUINodeState 
{
	NODE_STATE_ACTIVE,
	NODE_STATE_DORMANT,
	NODE_STATE_HIDDEN
}
